# วิธีหา Version Control ใน Replit

## 📍 ตำแหน่งของ Version Control UI

### วิธีที่ 1: Sidebar ด้านซ้าย
1. **ดูที่ sidebar ด้านซ้าย** ของหน้าจอ Replit
2. **หา icon ที่มีรูปแบบ:**
   - รูป Git branch (แยกกิ่ง)
   - รูป Source control
   - อาจจะเป็นไอคอนที่มี "Y" หรือ branch symbol
3. **คลิกที่ไอคอนนั้น**

### วิธีที่ 2: Menu หรือ Tools
1. **ดูที่ menu bar บนสุด**
2. **หา "Tools" หรือ "Version Control"**
3. **คลิกเพื่อเปิด Version Control panel**

### วิธีที่ 3: ใช้ Command Palette
1. **กด Ctrl+Shift+P** (หรือ Cmd+Shift+P บน Mac)
2. **พิมพ์ "version control" หรือ "git"**
3. **เลือก Version Control option**

### วิธีที่ 4: ใช้ Keyboard Shortcut
1. **ลองกด Ctrl+Shift+G** (shortcut ทั่วไปสำหรับ Git)

## 🔍 ถ้าหาไม่เจอ Version Control

### Alternative 1: ใช้ Git Tab
- หา **"Git"** tab ใน workspace
- อาจจะอยู่ด้านบนหรือด้านล่าง

### Alternative 2: ใช้ Console/Shell
- เปิด **Console** หรือ **Shell** tab
- รันคำสั่ง git โดยตรง

### Alternative 3: ใช้ Import from GitHub
1. **ไปที่ Replit Dashboard** (หน้าหลัก)
2. **คลิก "Import from GitHub"**
3. **ใส่ URL repository**
4. **คลิก Import**

## 🎯 ถ้ายังหาไม่เจอ

ลองดูที่:
- **Three-dot menu** (⋮) ที่มุมขวาบน
- **Settings** หรือ **Preferences**
- **View** menu เพื่อเปิด panels ต่างๆ

---

หากยังหาไม่เจอ แนะนำให้ใช้วิธี **Import from GitHub** แทน